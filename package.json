{
  "name": "neo-poker-server",
  "version": "1.0.0",
  "description": "Neo Poker multiplayer server (Node.js + WebSocket)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
